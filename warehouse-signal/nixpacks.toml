[phases.setup]
nixPkgs = ["python313"]

[phases.install]
cmds = ["pip install -r requirements.txt && pip install ."]

[start]
cmd = "uvicorn warehouse_signal.api.server:app --host 0.0.0.0 --port ${PORT:-8000}"
